#!/usr/bin/env groovy
@Library('shared-lib@master') _

script {
        addParameters {
                parameters = [
                    string(defaultValue: "TEST", description: 'What environment?', name: 'SERVICE_NAME')
                ]
        }

        stage("Smoke") {
                echo "Run smoke tests..."
        }

        stage("Acceptance") {
                echo "Run acceptance tests..."
        } 
}
